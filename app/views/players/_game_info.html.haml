%h4 Games Played (#{@player.games.count})

%table{width: '100%'}
  %thead
    %tr
      %th
        Winner
      %th
        Loser
      %th
        Date
  %tbody
    = render @games

= paginate @games, param_name: 'games_page'
